name: Manage stale PRs

on:
  schedule:
    - cron: "30 0 * * *" # will be triggered daily at 00:30 UTC.
permissions: {}
jobs:
  stale-prs:
    permissions:
      actions: write
      contents: write
      issues: write
      pull-requests: write
    uses: smartcontractkit/.github/.github/workflows/reusable-stale-prs-issues.yml@c036253c550f78925aac3160883e5774d0ded42c # 2025-06-05
    with:
      days-before-pr-stale: 30 # days
      days-before-pr-close: 7 # days
    secrets:
      GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
