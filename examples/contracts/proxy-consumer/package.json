{
  "name": "proxy-consumer",
  "version": "1.0.0",
  "description": "Simple contracts to read Chainlink data feeds on-chain and off-chain",
  "scripts": {
    "build": "scarb --profile release build",
    "deploy": "yarn ts-node ./scripts/deployConsumer.ts",
    "readLatestRound": "yarn ts-node ./scripts/readLatestRound.ts",
    "readLatestRoundOffChain": "yarn ts-node ./scripts/readLatestRoundOffChain.ts"
  },
  "license": "MIT",
  "dependencies": {
    "starknet": "^6.0.0-beta.11"
  }
}
